# Vite 在 build 时把 VITE_* 变量烤进打包后的 JS。
# 不设置时，前端会回退到 window.location.origin，自动适配你访问的 host/port。
# 仅在本地开发时（前端 5173、后端 8000 跨端口）需要显式指向后端：
#
#   cp .env.example .env.local
#   # 然后在 .env.local 里取消下行注释
#
# VITE_API_URL=http://localhost:8000
